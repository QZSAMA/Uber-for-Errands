<header class="intro-header" >
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
        <div class="page-heading">
          <h1>{{user.userName}}</h1>
          <span class="subheading">{{user.firstName}} {{user.lastName}}</span>
        </div>
      </div>
    </div>
  </div>
</header>

<div class="container">
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
      {{#each allPosts}}
        <div class="post-preview">
          <a href="/posts/{{../user.userName}}/{{this.post}}">
            <h2 class="post-title">
              {{this.title}}
            </h2>
          </a>
        </div>
        <hr>
      {{/each}}
      {{#if allPosts}}
        <ul class="pager">
          <li class="next">
              <a href="#">Older Posts &rarr;</a>
          </li>
        </ul>
      {{/if}}
    </div>
  </div>
</div>
